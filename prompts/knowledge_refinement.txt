Extract the core knowledge from the following reference documents. Output only the key bullet points that are relevant to answering the user's question. Do not add any content not present in the documents. Keep each bullet concise.

User Question: {query}

Reference Documents:
{context}

Additional constraint:
- If there are duplicated or highly similar bullet points, merge them and do NOT output repetitive bullets.

Output format (STRICT):
- Return a JSON object with a single key "bullets" whose value is a list of bullet point strings. For examaple, {"bullets":["clause1","clause2","clause3"]}
- Do NOT include any other keys.
- Do NOT wrap the JSON in markdown fences.

